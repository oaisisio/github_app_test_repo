# Welcome to the Repository
This is an initial commit by the GitHub App.

## Local Development Setup

1. Copy the example environment file:
   ```bash
   cp .env.example .env
   ```
2. Fill in real values for your local environment (Stripe test keys, local DB credentials, etc.)
3. Start the service: `npm run dev` (or equivalent)
