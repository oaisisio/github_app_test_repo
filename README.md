# Welcome to the Repository
This is an initial commit by the GitHub App.

## Local Development Setup

1. Copy the example environment file:
   ```bash
   cp .env.example .env
   ```
2. Fill in the real values in `.env` for your local environment (Stripe keys, database credentials, etc.).
3. Start the service as usual.