# Test environment configuration for trace query rewrite pipeline
# Used to validate wildcard pattern generation for Datadog trace queries

DATADOG_API_URL=http://localhost:8080/mock
DATADOG_APP_KEY=test_app_key
DATADOG_API_KEY=test_api_key
ENVIRONMENT=test
LOG_LEVEL=DEBUG
LLM_ENABLED=true
LLM_MOCK_RESPONSES=true
MAX_RETRY_ATTEMPTS=1
RETRY_INTERVAL_SECONDS=1
